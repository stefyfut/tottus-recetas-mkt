Enterno macOS

-- $ python3 -m venv myvenv
-- $ source myvenv/bin/activate
-- (myvenv) ~$ python -m pip install --upgrade pip
-- (myvenv) ~$ pip install -r doc.txt
-- (myvenv) ~/djangogirls$ django-admin startproject mysite .
-- (myvenv) ~/djangogirls$ python manage.py migrate
-- (myvenv) ~/djangogirls$ python manage.py runserver

-- pip freeze > req_lib.txt (genera archivo txt con los install necesarios)
-- cat req_lib.txt (lista dependecias a instalar)

Firebase Hosting

https://medium.com/@aleemuddin13/how-to-host-static-website-on-firebase-hosting-for-free-9de8917bebf2

firebase init hosting 
Hosting
firebase serve
firebase deploy


---> firebase use tottus-mkt-data
---> firebase deploy